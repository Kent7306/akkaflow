<coordinator name="coor_test" author="区广能" dir="/example" is-enabled="false">    
    <depend-list cron="*/1 * * * * *">
    </depend-list>  
    <trigger-list>
      	<workflow path="wf_test"></workflow>   
    </trigger-list>
    <param-list>
        <param name="yestoday" value="${time.today|yyyy-MM-dd|-1 day}"/>
        <param name="stadate" value="${time.today|yyyy-MM-dd|-1 day}"/>
    </param-list>
</coordinator>     
