package com.kent.workflow

/*class SubWorkflowNode(private var _name: String) extends ActionNode(_name) {
  def deepClone(): Node = {
    ???
  }
}

object SubWorkflowNode {
  def apply(name: String) = new SubWorkflowNode(name)
  def apply(name: String, node: scala.xml.Node) = parseXmlNode(name, node)
  
  def parseXmlNode(name: String, node: scala.xml.Node):SubWorkflowNode = {
    val sn = SubWorkflowNode(name)
    sn
  }
}*/